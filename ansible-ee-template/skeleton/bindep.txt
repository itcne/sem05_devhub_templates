# System packages required for this execution environment

# Core system dependencies - don't remove these
gcc [platform:rpm]
python3-devel [platform:rpm]
python3-pip [platform:rpm]

{% if values.systemPackages %}
{% if values.systemPackages is iterable and values.systemPackages is not string %}
{% for package in values.systemPackages %}
{% if package and package.strip() %}
{{ package.strip() }} [platform:rpm]
{% endif %}
{% endfor %}
{% endif %}
{% endif %}
